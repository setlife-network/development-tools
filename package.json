{
  "name": "setlife",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "clean": "lerna clean",
    "prebuild": "lerna exec -- rm -rf build",
    "build": "lerna build"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  }
}
