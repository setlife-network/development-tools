module.exports = [
<% for(var i = 0; i < mutations.length; i++) { %>
    {
        mutationName: '<%= mutations[i].name %>',
        tests: [
            {
                arguments: {<% for(var j = 0; j < mutations[i].args.length; j++) { %>
                    <%= mutations[i].args[j].name %>: <% if (mutations[i].args[j].testValue == null) { -%>null<% } else if (mutations[i].args[j].type == 'String') { -%>'<%=mutations[i].args[j].testValue%>'<% } else { -%><%=mutations[i].args[j].testValue%><% } -%>,<% } -%>

                },
                expectedResult: 'success'
            }
        ]
    },
<% } %>
]